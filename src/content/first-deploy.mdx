# 第一次部署

安装完成后，你需要进行第一次"部署"操作，让 Rime 编译配置文件并生成可用的输入方案。

## 什么是部署

部署是 Rime 将 YAML 配置文件编译为二进制数据的过程。每次修改配置后都需要重新部署才能生效。

## 如何触发部署

- macOS：点击菜单栏的输入法图标，选择"重新部署"
- Windows：右键点击系统托盘的小狼毫图标，选择"重新部署"
- Linux：根据前端框架的不同，使用对应的重新部署命令
