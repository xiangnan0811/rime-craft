# 候选词显示

控制候选词的排列方向和显示选项。

## 排列方向

- **竖排**（默认）：候选词从上到下排列，适合窄屏
- **横排**：候选词从左到右排列，节省纵向空间

## 拼音提示

`spelling_hints` 控制在候选词旁显示拼音提示的最大长度。设置为 0 关闭提示，设置较大的值（如 30）可以为更长的词组也显示拼音。

## 注释显示

`always_show_comments` 控制是否始终显示注释信息。关闭后仅在有辅助码筛选时显示注释。

<ConfigSlot module="candidate-display" />
