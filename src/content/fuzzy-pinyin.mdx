# 模糊音配置

模糊音功能允许你在输入时忽略某些发音差异，适合方言区用户或习惯性输入偏差。

## 常见模糊音对

- `zh ↔ z`、`ch ↔ c`、`sh ↔ s`：平翘舌不分
- `n ↔ l`：鼻音边音不分
- `an ↔ ang`、`en ↔ eng`：前后鼻音不分

## 配置方式

在输入方案的 `speller/algebra` 段中添加对应的模糊音规则，通过正则表达式将相似音节映射到同一输入码。
