# 按键绑定

Rime 提供了灵活的按键绑定系统，你可以自定义几乎所有的快捷键行为。

## 常用按键

- `Shift`：中英文切换（可配置）
- `Ctrl+~`：方案切换菜单
- `PageUp / PageDown`：翻页候选词

## 自定义按键

在 `key_binder` 配置段中，可以通过 `bindings` 列表添加自定义按键绑定。每个绑定由触发键、条件和动作三部分组成。
