# 输入方案管理

Rime 支持同时安装多种输入方案，你可以根据需要启用或禁用不同的方案。

## 切换输入方案

按下 `Ctrl+~`（macOS 为 `Control+~`）可以呼出方案选择菜单，在已启用的方案间快速切换。

## 配置可用方案

在 `default.custom.yaml` 中，通过 `schema_list` 字段指定要启用的方案列表。每个方案由其 schema_id 唯一标识。
